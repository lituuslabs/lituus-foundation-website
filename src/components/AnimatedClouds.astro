---
// Animated cloud layers with GPU-accelerated infinite scroll
---

<style>
  /* Cloud/Fog drift animation - GPU accelerated with transform */
  @keyframes fog-drift {
    from {
      transform: translateX(-100vw);
    }
    to {
      transform: translateX(0);
    }
  }

  .fog-layer {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 200vw;
    background-repeat: repeat-x;
    background-size: 100vw auto; /* Each tile = exactly viewport width for seamless loop */
    will-change: transform;
    transform: translateZ(0);
  }

  .animate-fog-slow {
    animation: fog-drift 120s linear infinite;
  }

  .animate-fog-fast {
    animation: fog-drift 60s linear infinite;
  }

  /*.fog-layer-fast { opacity: 0.4 }*/
</style>

<div class="absolute inset-0 w-full h-full pointer-events-none overflow-hidden">
  <!-- Cloud Layer 1 - Slow -->
  <div class="fog-layer animate-fog-slow bg-[url('/images/cloud-2.webp')]"></div>
  <!-- Cloud Layer 3 - Medium -->
  <div class="fog-layer animate-fog-fast bg-[url('/images/cloud-1.webp')]"></div>
</div>
