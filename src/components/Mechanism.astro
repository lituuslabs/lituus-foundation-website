---
// Mechanism section - "Our Approach"
// Custom icons with micro-interactions
---

<style>
  /* Target icon - locks on with rotation and pulse */
  .icon-target {
    transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  .icon-target .crosshair {
    transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    transform-origin: center;
  }
  .icon-target .center-dot {
    transition: all 0.3s ease-out;
    r: 1.5;
  }
  .group:hover .icon-target {
    transform: rotate(45deg);
  }
  .group:hover .icon-target .crosshair {
    stroke-width: 1.5;
  }
  .group:hover .icon-target .center-dot {
    r: 2.5;
    fill: currentColor;
  }

  /* Sprout icon - gentle sway animation */
  @keyframes sway {
    0%, 100% {
      transform: rotate(0deg);
    }
    25% {
      transform: rotate(-6deg);
    }
    75% {
      transform: rotate(6deg);
    }
  }
  .icon-sprout {
    transform-origin: bottom center;
  }
  .group:hover .icon-sprout {
    animation: sway 0.8s ease-in-out;
  }

  /* Globe icon - spins with orbital glow */
  .icon-globe-wrapper {
    position: relative;
  }
  .icon-globe-wrapper::before {
    content: '';
    position: absolute;
    inset: -4px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(209, 183, 195, 0.3) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.4s ease-out;
  }
  .group:hover .icon-globe-wrapper::before {
    opacity: 1;
  }
  .icon-globe {
    transition: transform 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  .group:hover .icon-globe {
    transform: rotateY(180deg);
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .icon-target,
    .icon-target .crosshair,
    .icon-target .center-dot,
    .icon-sprout,
    .icon-globe,
    .icon-globe-wrapper::before {
      transition: none;
      animation: none;
    }
    .group:hover .icon-target,
    .group:hover .icon-sprout,
    .group:hover .icon-globe {
      transform: none;
      animation: none;
    }
  }
</style>

<section id="mechanism" class="relative py-32 z-10">
  <div class="max-w-7xl mx-auto px-6">
    <div class="mb-20 flex flex-col md:flex-row md:items-end justify-between gap-8 border-b border-white/10 pb-8">
      <div>
        <span class="text-xs tracking-[0.2em] text-lituus-pink uppercase mb-4 block">
          Our approach
        </span>
        <h2 class="text-4xl md:text-5xl font-light text-white">
          Empower <span class="font-serif italic text-lituus-pink">Thriving</span> Communities
        </h2>
      </div>
      <p class="text-slate-300 font-light max-w-sm lg:max-w-lg md:text-right">
        We believe in people coming together to build what they need—often the closest to the problem find the best solutions.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <!-- Card 1: Strategic Resource Allocation -->
      <div class="glass-panel rounded-sm p-10 hover:-translate-y-2 transition-transform duration-500 group">
        <!-- Target/Crosshair Icon - locks on with rotation -->
        <svg class="icon-target w-7 h-7 text-lituus-pink mb-8" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <!-- Outer circle -->
          <circle class="crosshair" cx="12" cy="12" r="10"/>
          <!-- Inner circle -->
          <circle class="crosshair" cx="12" cy="12" r="6"/>
          <!-- Crosshair lines -->
          <line class="crosshair" x1="12" y1="2" x2="12" y2="6"/>
          <line class="crosshair" x1="12" y1="18" x2="12" y2="22"/>
          <line class="crosshair" x1="2" y1="12" x2="6" y2="12"/>
          <line class="crosshair" x1="18" y1="12" x2="22" y2="12"/>
          <!-- Center dot -->
          <circle class="center-dot" cx="12" cy="12" r="1.5" fill="currentColor"/>
        </svg>
        <h3 class="text-2xl font-serif text-white mb-4 group-hover:text-lituus-pink transition-colors">
          Strategic Resource Allocation
        </h3>
        <p class="text-sm font-light text-slate-300 leading-7">We allocate resources to projects most critical to oracle resilience, security, and usability. This includes funding independent development teams exploring different approaches to the same fundamental problems.</p>
      </div>

      <!-- Card 2: Nurturing Communities -->
      <div class="glass-panel rounded-sm p-10 hover:-translate-y-2 transition-transform duration-500 group relative overflow-hidden">
        <div class="absolute top-0 right-0 w-32 h-32 bg-lituus-pink opacity-5 blur-[60px] rounded-full"></div>
        <!-- Sprout/Seedling Icon - gentle sway on hover -->
        <svg class="w-7 h-7 text-lituus-pink mb-8" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <!-- Ground line (static) -->
          <path d="M2 22h20"/>
          <!-- Plant (sways) -->
          <g class="icon-sprout">
            <path d="M12 22v-9"/>
            <path d="M12 13C12 13 8 11 5 8c0 4 3 7 7 9"/>
            <path d="M12 9C12 9 16 7 19 4c0 4-3 7-7 9"/>
            <circle cx="12" cy="6" r="2"/>
          </g>
        </svg>
        <h3 class="text-2xl font-serif text-white mb-4 group-hover:text-lituus-pink transition-colors">
          Nurturing Communities
        </h3>
        <p class="text-sm font-light text-slate-300 leading-7">
          We encourage active participation in project governance, open discussion of technical decisions, and transparent processes that give voice to everyone contributing to the ecosystem.
        </p>
      </div>

      <!-- Card 3: Open Source Everything -->
      <div class="glass-panel rounded-sm p-10 hover:-translate-y-2 transition-transform duration-500 group">
        <!-- Globe Icon - spins with glow -->
        <div class="icon-globe-wrapper w-7 h-7 mb-8">
          <svg class="icon-globe w-7 h-7 text-lituus-pink" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <circle cx="12" cy="12" r="10"/>
            <path d="M2 12h20"/>
            <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
          </svg>
        </div>
        <h3 class="text-2xl font-serif text-white mb-4 group-hover:text-lituus-pink transition-colors">
          Open Source Everything
        </h3>
        <p class="text-sm font-light text-slate-300 leading-7">
          All supported work is free and open-source, contributing to a commons that strengthens the entire ecosystem.
        </p>
      </div>
    </div>
  </div>
</section>
